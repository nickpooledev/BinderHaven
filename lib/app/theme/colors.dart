/// Centralized application color definitions.
///
/// All colors used throughout BinderHaven should originate here.
/// UI code should never reference raw Color values directly.
class AppColors {
  const AppColors._();
}