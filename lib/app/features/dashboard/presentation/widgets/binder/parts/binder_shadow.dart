import 'package:flutter/material.dart';

class BinderShadow extends StatelessWidget {
  const BinderShadow({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}