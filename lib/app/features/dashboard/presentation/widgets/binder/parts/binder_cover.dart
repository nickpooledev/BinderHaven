import 'package:flutter/material.dart';

class BinderCover extends StatelessWidget {
  const BinderCover({super.key});

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}